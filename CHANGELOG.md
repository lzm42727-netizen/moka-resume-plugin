# Changelog

## 1.8.8 - 2026-09-10

- **结果页首行改版**：原「用量：LLM 调用 3 次 · 输入 7,820 / 输出 16,656 tokens · 约 ¥0.156」改为「**用时 2 分 41 秒 · 预估花费 ¥0.156**」——调用次数与 token 是排障信息，收进运行日志与筛选中的进度行，结果页只留日常关心的耗时与费用
- **用时**：整轮口径，从点「开始筛选」（含拉列表准备）起算，秒为最小单位（48 秒 / 2 分 41 秒 / 1 时 2 分 3 秒）；筛选中每次出分实时刷新，完成/停止时冻结不再走动；续筛从任务快照起点续算，跨刷新不断账
- **估不了费时整行隐藏**：模型未收录单价且未填自定义单价时不再显示该行（用时也不单独展示）
- 测试 453 → 459（costOnlyText、formatElapsedText、快照组装与冻结口径）；`npm run check` 全绿

## 1.8.7 - 2026-09-10

- **降档抗抖**：并发自适应此前「一次超时/过载就降一档」，一次网关抖动（偶发 429 或单次超时）就会把并发从 6 降到 4，看起来像「设置没生效」。现在改为**连续 2 次**过载/超时信号（中间无成功）才降一档；任一次正常出分立即清零失败计数，间歇抖动不再累积误降
- **并发可见**：开筛/续筛时运行日志会写一行「评分并发 N（设置上限 M）」，若当前已按网关反馈降档会追加「· 已按网关反馈降档」——设 6 实际跑 4 的原因一眼可见
- 测试 450 → 453（降档抗抖、成功打断失败计数、开筛并发日志）；`npm run check` 全绿

## 1.8.6 - 2026-09-10

- **修复「续筛 + 重新开筛」双跑**：续筛链路此前不持有本轮 epoch，导致点「开始筛选」后旧的续筛 worker 不会停手——同一候选人被两条链路各评一遍（日志里同一人两次评分、token 数不同），实际并发翻倍把网关打到 503，随后连锁超时（实测一次筛选里大半候选人变「错误 0 分」）。现在续筛也持 epoch，新一轮开筛立即叫停旧 worker
- **单次调用超时 90s → 150s**：思考型模型（M2 等）单次评分真实耗时可能 60–150s，90s 会把正常调用判死
- **超时最多重试 1 次（原 3 次）+ 5s 退避**：此前「3 次 HTTP 重试 × 内容侧 3 轮重试」最多 9 次调用，网关一过载就被重试打得更狠；现在超时不再走通用重试，失败交给筛选末尾统一补评
- **并发自适应**：新增信号量闸门——遇到网关 429/5xx 或超时自动降一档（8→6→4→3→2），连续成功 10 次回升一档，降/升都会写运行日志；设置页的并发数作为上限
- **失败分类回传**：调用层异常归成 timeout / overload / network / config / other 并随结果回包，评分日志直接写「失败类型：timeout」，不再是笼统的「错误（0 分）」；超时/过载/网络/配置类失败不再在单卡内反复重试
- 内容侧单次评分等待上限 120s → 330s（对齐后台 150s 超时 + 一次重试，避免侧栏早于后台判超时）
- 测试 439 → 450（续筛 epoch、超时限量重试、失败分类、并发档位与信号量、诊断文案）；`npm run check` 全绿

## 1.8.5 - 2026-09-10

- **评分输出上限 4000 → 8000 tokens**：思考型模型（MiniMax-M2 等）的思考内容也计入这份输出预算，4000 常被思考挤满导致 JSON 截断——这是「模型返回不是可解析的 JSON」的主要成因
- **截断自动加倍重试**：识别 `finish_reason=length`（输出被截断）且解析失败时，用 16000 上限再调一次；两次调用都计入本轮用量与费用（`meta.calls`）
- **自定义/中转端点默认带 JSON 模式**：`response_format: json_object` 从「仅官方 OpenAI」放开到自定义端点，设置页新增「强制 JSON 输出」开关（默认开）；网关不支持（4xx 拒绝）时自动去掉该参数降级重试一次，并写日志提示，不再让整批候选人一起失败
- **未闭合思考块剥离**：输出在思考阶段就被截断（`<think>` 没闭合）时，不再把思考文本当成 JSON 去解析
- **评分并发数可配置**：设置页「评分并发数」可选 1–8（默认 6，原为硬编码 4）；自建网关/本地模型可调大提速，被限流时调小
- **评分诊断日志**：每条评分日志追加「输出 N 字 · 截断(length) · 含思考 · N 次调用 · 解析失败：类型」，慢与失败一眼定位
- **修复单人「重评」必然失败**：`rescoreItem` 把 `{score, usage, meta}` 整个包装对象当成模型输出传给合成函数，导致重评结果永远判成「评分失败」；现取 `.score`，并把重评产生的用量并入本轮统计
- 测试 423 → 439（JSON 模式与降级、截断重试、finish_reason 解析、未闭合 think、并发夹取、诊断文案、重评取 score、用量按实际调用次数计）；`npm run check` 全绿

## 1.8.4 - 2026-09-10

- **结果卡右列「未体现」改名「差距」**：右列实际混装三类内容——确认未过的门槛（简历写了但不满足）、简历里找不到证据的 JD 要求、信息不足的「待确认」项——「未体现」只说对了其中一层；「差距」一个词全罩住，与左列「亮点」正反对仗。细粒度区分仍由列内徽章承担（红「门槛」/琥珀「待确认」/✕）
- 档位行合并提示同步为「未过门槛 N 项（见差距）」；名字下方的加分项标签「加分未体现」保持不变（该处语义确为「简历未体现此加分项」）
- 同步更新 README / 使用说明 / 插件介绍 的对照卡名称与示例（插件介绍顺带补齐 1.8.3 的「具备→亮点」对齐）；既有用例内新增右列名称锁定断言（总数 423 不变）；`npm run check` 全绿

## 1.8.3 - 2026-09-10

- **评分落卡动效**：筛选中某位候选人首次出分时，卡片内容 180ms 淡入上移、分数徽章轻弹入——只在「评分中→已出分」切换时播一次，筛选/搜索重绘不重播，新一轮筛选自动重置；系统开启「减弱动态效果」时整体禁用
- **进度条运行态流光**：筛选进行中底部进度条加低对比流光扫过表示「活着」，结束后自然静止（同样受减弱动态设置约束）
- **结果卡「具备」列改名「亮点」**：与列内容（AI 提炼的亮点）及空态文案「AI 未找到与岗位直接相关的亮点」对齐，读起来更直接；「未体现」对照不变
- **操作按钮 emoji 换单色 SVG**：API Key 眼睛（睁/闭用 class 切换）、日志暂停/清空/复制/导出五个按钮换 14px 线性图标，全 UI 不再有 emoji
- 测试 421→423（亮点改名 / 落卡动效只播一次 / reduced-motion 守卫 / 无 emoji 残留断言）；`npm run check` 全绿

## 1.8.2 - 2026-09-10

- **配置页去卡片壳**：五个分区（职位信息/简历推荐对象/岗位理解/硬性门槛/筛选关键词）不再各套一层白卡边框，改为「图标 + 标题 + 细分割线」的连续分区——层级更清楚，内容区高度多出约 8–10%；分区内容、顺序、功能全部不变，卡片质感只留给底部操作区和结果卡
- **标题图标 emoji → 单色线性 SVG**：📋🎯💡🔒✅🔗🛠️ℹ️ 八个分区图标换成 16px 线性 SVG（跟随文字色，跨系统渲染一致），不再有彩色圆底
- **字号地板 10px → 11px**：标签、用量行、日志计数、门槛徽章等所有 10px 文字统一抬到 11px，密集侧栏里不再吃力
- **数字等宽对齐**：决策分、进度计数、用量、日志计数等数字加 `tabular-nums`，多行数字纵向看数不漂移
- 测试 420→421（去卡片壳/无 emoji 残留/扁平分区断言替换旧卡片断言）；`npm run check` 全绿

## 1.8.1 - 2026-09-10

- **结果卡瘦身：一个卡只留一个分**。卡面原来并排出现决策分 28、档位旁的「经历匹配 57」、评分明细里的四项分，招聘官不知道看哪个。现在卡面只留决策分；匹配分与四项分收进「评分明细」，开头两行讲清计算链——`匹配分 57 ＝ 52×40% + 55×25% + 62×20% + 66×15%`、`决策分 28 ＝ 49 − 7 × 3 条未过门槛`，所有数字的来龙去脉一处讲完
- **去掉重复的门槛标签行**。顶部那 3 个红色「未过门槛」标签和「未体现」列前三条是同一份数据渲染两遍；现删除标签行，档位行合并成一句「不建议推进 · 未过门槛 3 项（见未体现）」，门槛明细只在「未体现」列出现一次
- **未体现的门槛条目改「门槛」徽章**。原本每条都带「未过门槛「xxx」（原因）」长前缀，3 条下来很啰嗦；现改成一个红色「门槛」小徽章 + `条目：原因`，与非门槛的未体现项（保持 ✕）一眼可分
- 旧缓存结果（无四项 scoreBreakdown）回退保留「经历匹配 57」展示，不会开天窗；加分提示文案同步去掉冗余的「经历匹配」前缀
- 测试 415→420（matchScoreDisplayText 结构化分支 / bonusScoreDisplay 去前缀 / buildEvidenceColumns 门槛文案 / popup 门槛行删除 + 计算链渲染）；`npm run check` 全绿

## 1.8.0 - 2026-09-09

- **评分体系重构：四项结构化 + 本地加权合成**。模型不再直接拍一个总分，而是分别给「核心职责 40% / 业务场景 25% / 专业技能 20% / 责任范围 15%」四项打分并各带理由，插件按固定权重在本地合成决策分（权重不再交给模型，避免同一份简历分数漂移）。职位名、公司名、学历、年限、关键词都不能替代职责证据
- **核心职责保护封顶**：核心职责分是最高权重项，低于阈值时其他维度再高也压不过——`coreDuty < 40` 封顶 49、`40–59` 封顶 69、`60–79` 封顶 84，`≥ 80` 不封顶。防止「title 很像但实际干的活不对口」被高分误推
- **门槛三态（pass / fail / unknown）**：简历没有提及某项时标为 unknown——不脑补、不直接判不具备，也**不扣分**；只有明确反向证据才判 fail。卡片上用琥珀色「待确认 · ×××」标出需要人工核对的项，与红色「未过门槛」区分
- **卡片新增两个把握度指标**：卡面常显「简历信息完整度 N%」（简历中可核对信息占岗位关键要求的比例）与「判断把握 高/中/低」（模型对本次判断的把握，受完整度影响）；四项分数与理由收进「评分明细」折叠区，卡面只留结论，避免信息过载
- **修复：四维只回一部分时静默合成 0 分**。此前模型若只返回一两项（如 4 项只给 2 项），残缺数据会被当成有效结果、综合分静默算成 0，卡片显示「0 分」而不是「评分失败」，招聘方会误以为是真实评价；现在四维不完整一律判为评分失败并走重试/补评
- **修复：卡片四项分渲染成 undefined**。合成结果里四项分原本透出的是扁平数字，而卡片按 `{score, reason}` 取数，导致分数与理由都取不到；已统一输出形状
- 评分提示词版本 `evidence-first-bonus-cal-v5` → `evidence-first-balanced-v1`：旧评分缓存自动失效，已评过的候选人需重评一次才会用上新规则（不重评则沿用旧分）
- 测试 408→415（新增四维加权合成 / coreDuty 封顶 / unknown 三态不扣分 / scoreBreakdown 形状 / 残缺四维判失败等断言）；`npm run check` 全绿

## 1.7.5 - 2026-09-09

- 点卡片打开候选人简历不再每开一个新标签页：固定复用同一个「简历详情」标签（首次点击新开，之后都在这一个标签里刷新），逐个看人不再堆积标签页；手动关掉后再点会重新开一个

## 1.7.4 - 2026-09-09

- **筛选结束自动补评**：主轮结束后若仍有评分失败（多为模型偶发输出异常），自动把失败项整体再补评一轮（只补一轮不递归，仍失败的落卡等手动「重评」），日志与进度条可见补评过程
- **重试带纠偏指令**：解析类失败触发的自动重试，会在评分指令末尾附加「严格只输出一个 JSON 对象，不要思考过程 / 解释 / markdown」——提高第 2、3 次重试成功率；纠偏指令不参与评分缓存 key，评分标准未变、既有缓存不受影响（不 bump PROMPT_VERSION）
- 评分调用本就以 `temperature: 0` 运行（确定性输出），无需改动

## 1.7.3 - 2026-09-09

- 筛选进度口径统一：底部进度条原本只数「有效评分」（评分失败的 6 个人不计入），与结果页顶部「评分 N/M」（含失败）对不上，看起来像进度卡住；现在两处都按「已处理（含失败）」计数，数字完全一致
- 进度文案修正：并发评分时「正在评 ×××」显示的其实是刚评完的一位，不是正在评的；改为「已评 57/82 · 刚完成 ××× · 已用约 N 分钟」，不再误导
- 断点续筛恢复文案（「恢复筛选：继续评分（已完成 N/M）」）同步换用同一口径

## 1.7.2 - 2026-09-09

- 已决策 CSV 导出增强：新增「同步状态」（已同步 / 同步失败 / 待同步）与「决策时间」两列——普通筛选结果导出（非已决策）时这两列留空，不误导；导出文件名带职位名（`moka-已决策-{职位}-{日期}.csv`），多职位分别导出不再同名覆盖/难找

## 1.7.1 - 2026-09-09

- **P1-9 桥接消息 nonce 握手（安全）**：content 生成一次性 nonce 下发给 inject，inject 之后每次上行 push 统一回带；握手确认后 content 只采信带正确 nonce 的消息——页面内其它脚本伪造 moka-inject 来源的写操作（篡改经历 / 分配映射 / 重放模板）会被丢弃。确认前（旧 inject / 时序窗口）不校验，兼容不断链；detail-data 本身已有「合法 JSON + URL 含 application id」结构校验兜底
- **P1-10 存储写入失败可观测**：background 新增 `storeSet` 统一封装（读 lastError / 捕获异常并 console.error）；LLM 缓存落盘与默认设置初始化改走它——缓存写失败不再静默丢（否则 SW 重启会重复扣费）。popup 侧「保存岗位配置」「保存并测试」本就已 try/catch 兜底并提示失败，未重复改
- **P2-4**：keepalive alarm 空闲自清——SW 重启后 `keepaliveTabId` 归 null 但周期 alarm 仍在时清掉，不再每分钟空唤醒
- **P2-9**：enrichCandidate 详情抓取加 1 次重试（断网/超时/429/5xx 短暂退避，其余 4xx 立即换下一条 URL）；requestAssigneePairs 超时 4s → 6s
- **P2-12**：运行日志落盘 / 实时广播的静默吞错收敛为 console.warn，失败可见
- 测试 401→403（bridge-contract nonce 握手 ×1 / background-contract storeSet+alarm ×1）；npm run check 全绿

## 1.7.0 - 2026-09-09

- **P2-1 工程门禁扩围（防 1.6.17 类事故复发）**：`npm run lint` 由 `lib tests scripts` 扩到 **content.js / background.js / inject.js / popup/popup.js 四个入口文件**（--max-warnings=0 不变）；eslint.config.js 为入口文件单独配浏览器 / service worker 环境 globals + Moka* 命名空间白名单，`no-undef` 直接锁死「引用未声明标识符」（正是 setMultiSelectOptions 死调用能漏网的同类形态）；新增 tests/entry-lint-gate.test.js 钉死入口必须在 lint 范围，防止以后被悄悄拿掉
- 死代码清理（lint 上入口后首批暴露的 16 处孤儿，均已全仓核实无引用后删除）：background.js 删 setPendingMokaForTab/getPendingMokaForTab/persistPendingMokaToDisk（逻辑已被 Async 版取代）、neutralDimensions；content.js 删 normalizeAgeRanges/buildHardText/stripHtml/formatExperienceList（lib 薄包装）、deepFindAll/buildDetailUrl（lib 同功能）、rescoreItem 里未使用的 weights 赋值；popup.js 删 maybeFillEmptyRequirementsFromJd 空 stub、switchResultTab、setCandidateFeedback，applyRequirementsToEditors 去掉从未使用的 hard 参数；inject.js 删从未使用的 seenFiber
- **P2-6 XHR load 监听防泄漏**：inject.js 劫持 send 时挂的 load 监听改为具名函数，触发后首行 removeEventListener 自移除——同一 XHR 实例复用 send 不再累积监听
- **P2-10 锁状态解锁补洞**：reconcileJobTypeWithLabel（按职位名校正实习/正式）的 applyingPreset 改为 try/finally 复位——与 1.6.17 的锁卡死同形态的隐患点，统一消除
- **P2-11 顶层 DOM 绑定空值守航**：popup.js 顶部新增 safeEl(id)（缺元素 console.warn 一次并返回 null）；reload-panel/api-provider/toggle-api-key/save-settings/start-screening/stop-screening/save-job-preset 及 bindResultFilters 内 result-search/export-results 等关键绑定全部改走 safeEl/可选链——HTML 重构漏掉单个 id 只跳过该绑定，不再整段瘫痪
- 测试 396→401（entry-lint-gate ×3 / popup-ui safeEl ×1 / bridge XHR 自移除 ×1）；npm run check 全绿。prettier 范围暂未扩（入口文件整文件重排 diff 过大，留待单独格式化）

## 1.6.20 - 2026-09-08

- 分页抓取加重试退避（P1-5）：`fetchAllApplications` 单页请求对超时/断网/HTTP 429/5xx 做指数退避重试（最多 3 次，600ms 起、上限 6s、带抖动），其余 4xx（会话失效等）立即失败不空转；筛选被停止时中止重试优雅收尾。此前任一页偶发超时/限流都会让整轮抓取（已完成页结果）全部作废
- 热更新幂等守卫（P1-6）：inject.js 顶部加 `window.__mokaInjectLoaded` 一次性守卫，fetch/XHR 劫持各自带 `__mokaWrapped` 标记，重复注入不再二次包裹页面请求、不叠加消息监听；content.js `init()` 加 `contentBooted` 守卫，`startPageContextWatch` 的 popstate 只绑一次（interval 本就先清再设）
- importScripts 逐个容错（P1-7）：background.js 七个 lib 改为逐个 `safeImportScripts()` try/catch 加载，单个文件加载失败不再中断后续脚本，SW 仍能启动且 console.error 定位到失败文件（config.local.js 维持静默忽略）
- 并发评分 in-flight 去重（P1-8）：`handleScoreCandidate` 对同一 cacheKey 的并发请求共享同一次 LLM 调用，结果一致且不产生第二次扣费（共享方 meta 标记 cacheHit/inFlightShared，不重复累计 usage）；成功/失败都会移除占位，保证换参/重试可重新发起
- 契约收口：`flushPluginLog`（popup 发送、content 处理、此前漏登记）补进 ACTIONS；新增 `BRIDGE_UP`（inject→content 10 类上行）/ `BRIDGE_DOWN`（content→inject 4 类下行）与两侧 source 常量，作为 MAIN↔ISOLATED 桥接的权威登记表；content 侧 hasLastResults/showLastResults/getRequestLog/getBatchAssignContext 四个动作 popup 已不再发送，契约保留登记、content 保留处理作数据源与旧版兼容
- 测试：新增 bridge-contract.test.js（契约对称校验：ACTIONS 登记 / 上行下行类型 inject 发、content 收一一对应 / source 对称）；background-contract 增补 safeImportScripts 容错用例并更新 importScripts 断言

## 1.6.19 - 2026-09-08

- 修复「左侧切换完岗位后岗位理解/门槛关键词不自动填充」的真正根因（快照实锤 A↔B 交叉错位）：切岗保存「离开岗」存档时，职位名锚取的是**已经切到的新岗名字**——下拉 change 触发时 select 已更新，函数内读 `currentJobLabel()` 会把新岗名盖到旧岗 key 上（如 ba2d225a 存成 JAVA 名、efaa1e46 存成 Golang 名）。现 `switchJobPreset` 增加 `prevLabel` 参数、`saveJobPresetFor` 支持显式 `opts.label`，离开岗存档一律用旧岗名盖锚
- 空壳存档防线：恢复时判定存档是否有可用内容（门槛/关键词/理解/jobSpec 任一非空）；命中纯空档视为「未恢复」（诊断 hit=empty），按首次进入自动重新生成并保存——此前空壳被按名兜底选中后会显示空表单，还挡住自动生成路径（本次损坏的两岗存档即空壳）
- 同名多份存档恢复时优先选「有可用内容」的最新一份，全为空壳才退回空档处理；空壳命中保留原「确认章」，重新生成落盘不丢简历推荐对象确认状态
- 切岗保存旧岗前先检查表单：无可保存内容（空表单/前一轮恢复失败残留）直接跳过保存，杜绝空白覆盖真实存档

## 1.6.18 - 2026-09-08

- 切岗加互斥队列：`switchJobPreset` 的「保存旧岗 → 清空表单 → 恢复新岗」整段串行执行。此前手动切岗（下拉 change）与 loadJobs（页面 SPA 切岗）可并发触发，快速 A→B→C 切岗时后一轮保存可能在上轮刚清空、还没填回 B 内容的窗口期执行，把空/半成品表单覆盖进 B 的真实存档（跨岗串档同源）；手动切岗失败不再产生未处理 rejection，状态行给出提示
- 侧栏初始化分步容错：window load 启动链逐项 try/catch，任一步存储/消息异常只跳过该步并 console.warn，不再拖垮后续——此前一次 storage 读取异常会让结果区搜索/筛选/导出的事件绑定（bindResultFilters）永不执行、功能半残且无提示
- 修复「停止/暂停后被在途 worker 把任务状态写回 running」：用户点停止或页面切岗触发暂停的瞬间，仍有 worker 卡在评分请求（最长 120s），返回后 `patchScreeningJob({status:'running'})` 会把刚冻结的 stopped/paused_mismatch 覆盖回 running，导致任务误判仍在筛、断点续筛错误恢复。现 patchScreeningJob 增加会话守卫：`isScreening=false` 时进度心跳只更新 completed/usage 等字段、不改状态

## 1.6.17 - 2026-09-08

- 修复「保存成功、再次进入仍不自动填充（只剩学历回填，重点看/加分看/岗位理解全空）」的真正根因：v1.6.12 把年龄改为下拉式多选（details 壳 + 胶囊勾选）时，`writeHardConditions` 里残留了对已随迁移删除的 `setMultiSelectOptions('cond-age', …)` 的调用——该函数全工程从未定义，任何一次「恢复/重置表单」都会在写入学历之后抛 `ReferenceError`，中断 applyJobPreset 里后续的关键词芯片与岗位理解渲染；而保存路径不经过该函数，所以「保存永远成功、回填永远半截」
- 防御加固：`applyJobPreset` / `resetJobPresetForm` 改为 try/finally 包裹，中途抛错也必定复位 `applyingPreset` 锁，杜绝「一次异常后所有自动保存被静默拒绝」的二次故障
- 恢复异常兜底：`restoreCurrentJobPreset` 捕获恢复渲染异常，记录 applyError 到诊断并降级提示，不再让异常静默中断进岗链路
- 排查快照增强：presets 行新增 reqImportant（恢复实际读取的 requirements.important 条数）与 hasUnderstanding 字段，与 focusCount 并列输出，今后可分辨「存档缺关键词」与「回填没执行」

## 1.6.16 - 2026-09-08

- 修复跨岗串档（快照实锤：商务运营实习生的存档键下锚名是「系统研发工程师」）：快速切岗期间，防抖保存/切岗保存/确认章兜底可能把 A 岗表单内容写进 B 岗存档键，此后每次进入都「精确命中」脏存档，回填出错误内容或空门槛
- 保存加跨岗防污染闸门：表单未绑定职位或绑定的是别的职位时拒绝落盘（含空表单覆盖真存档的防护）；保存按钮被拦截时明示原因；「确认本岗简历推荐对象」只盖确认章，不再用当前表单内容兜底覆盖存档
- 恢复加串档防线：存档键命中但身份锚名与当前职位不符时拒绝自动填充（诊断 hit=anchor-mismatch），状态行提示重新配置保存覆盖；脏存档由下一次正常保存自然洗净
- 修复职位类型串档：恢复存档后按职位名强信号校正（名含「实习」强制回实习生），实习生存档不再以「正式员工」形态出现

## 1.6.15 - 2026-09-08

- 「保存了却不自动回填」排查能力：每次存档恢复尝试记录诊断（目标职位 id/名、全部存档 key 与锚点名、命中方式 exact/name/none/no-job）；未命中时配置页状态行直接显示诊断摘要（存档条数/同名命中数），jobId 为空时明示「未识别到当前职位」而不再静默跳过
- 排查快照追加 presetRestore（最近一次恢复诊断）与 presets（全部职位存档锚点清单：key/锚 id/锚名/时间/是否含理解），凭一份快照即可定位回填链路断点

## 1.6.14 - 2026-09-08

- 全面修复「关闭插件 → 刷新 Moka → 重开插件，同一职位不自动带出上次存档的岗位理解/门槛/简历关键词」：此前存档的「职位身份」只有 jobId 键与按 JD 刷新产物里的 sourceJobName，纯手配门槛/关键词的存档没有职位名锚，页面 jobId 一漂移就彻底找不回
- 存档新增顶层身份锚点 jobIdAnchor / jobNameAnchor：每次保存都从侧栏当前职位盖章（不再依赖 AI 解读产物）；老存档自动回退 jobSpec 来源戳，保证旧数据也能按名恢复
- 恢复改为三级兜底：按当前 jobId 精确命中 → 按职位名取同名中 savedAt 最新一份 → 仍无则提示未保存；不再因「同名存档多份」误判为空
- 已有存档无需迁移：读取时自动补齐锚点语义（jobSpec 来源名回退），后续任意一次保存即盖上新锚

## 1.6.13 - 2026-09-08

- 修复「保存当前筛选条件成功后，刷新 Moka 页面再进同一职位却提示未保存/空态」：存档 key 用的是页面 jobId，页面刷新或从不同入口进入后该 id 可能漂移；按职位名兜底恢复时此前要求同名存档必须唯一，多轮测试留下的旧 id + 新 id 多份同名存档会令兜底失效。现改为同名多份时取 savedAt 最新的一份恢复，不改存档结构、不删旧数据

## 1.6.12 - 2026-09-08

- 概念改名：「分配对象」→「简历推荐对象」（配置页标题、确认/采纳按钮、状态行、批量推进面板、运行日志推荐分类的请求条目与说明、排查快照说明、未记录引导等文案同步调整）；职位存档字段与代码内部标识不变，Moka 页面自身的「分配对象」标签匹配不受影响
- 年龄要求改为下拉式多选：硬性门槛网格内「实习经验右侧（性别要求下方）」的空位新增「年龄要求」，收起时与其他下拉同外观并显示已选档位（默认「不限」），点击展开可复选 20-25 / 25-30 / 30-35 / 35 岁以上四档胶囊，点击面板外自动收起
- 旧 35-40 / 40-50 / 50+ 档位合并进「35 岁以上」：已存预设/自动填充回填时自动归一映射，≥35 筛选语义不变
- 院校要求移除「QS500」选项（保留 211 / 985 / 双一流 / 留学生 / QS100 五档）；JD 自动填充遇 QS500 提取值时不再展示与提示，已存预设中的 QS500 自动忽略

## 1.6.11 - 2026-09-08

- 配置页卡片化：五个分区从「横线分隔的连续流」改为独立浅色圆角卡片（含细边与轻阴影、卡片间距），标题图标加圆形浅色底、按职能分区着色（职位蓝/分配紫/理解橙/门槛青/关键词绿），只动样式不影响任何逻辑
- 硬性门槛两列化：学历|性别、经验|实习 四个下拉两两并排（网格布局），院校/年龄/语言/专业多选类保持整行；门槛区纵向占用明显减少
- 提示文案降噪：职位信息/岗位理解/筛选关键词的长说明压缩为一行核心，完整规则移到 hover 悬浮（title）展示；硬性门槛标题注明「空项不参与筛选」

## 1.6.10 - 2026-09-08

- 设置页「连接与模型」降噪分层：API Endpoint 只在选「自定义 API」时显示（OpenAI/Claude 隐藏，少一行噪音）；模型名称等说明文字精简
- 「高级 · 费用估算单价」收进折叠区（默认收起），需要自定义单价再展开
- 「测试 API」与「保存设置」合并为「💾 保存并测试」：保存成功后自动测一次连接，一次点击完成两件事
- 运行日志工具栏图标化：暂停/清空/复制/导出改为无边框图标按钮（hover 出提示），筛选 chips 独占一行；暂停变为可按下开关（aria-pressed）
- 设置页整体节奏微调：组间距、说明文字字号、隐藏组兜底样式统一（仅作用于设置页，不影响其他标签页）

## 1.6.9 - 2026-09-08

- 排查工具去重：移除独立的「复制接口流水」按钮（接口流水本就完整包含在排查快照内），排查工具折叠区只留「🧾 复制排查快照」一个入口
- 排查快照文案说明与运行日志的分工：运行日志面板看行为轨迹（人读），排查快照导出机器可读完整数据（页面上下文 + 职位名映射 + 分配存档 + 完整请求流水）
- content 端 `getRequestLog` 数据源保留（运行日志「请求」条目与快照流水同源），仅不再单独暴露按钮

## 1.6.8 - 2026-09-08

- 移除「筛选完成」桌面通知：去掉 notifications 权限与相关设置项，改为侧栏内联完成提示（不打断、无权限弹窗）
- 设置页改版为两张卡片：「连接与模型」+ 全新「运行日志」面板，分区更清晰
- 新增插件运行日志（会话级，storage.session 留存最近 100 条，关闭浏览器即清空）：content 把接口请求、分配对象采纳、筛选生命周期（开始/续筛/停止/暂停/完成）、逐人评分摘要、错误与重试统一上报 background 沉淀
- 日志面板支持实时滚动、分类筛选（全部/请求/分配/筛选/评分/错误，错误含可恢复重试警告）、暂停跟读、清空、复制为文本、导出 .txt
- 旧「接口观测」降级为折叠的「排查工具」，保留复制接口流水 / 复制排查快照（分配对象）两个入口
- 清空/查看日志前先冲刷各 Moka 页面的本地日志队列，避免清空后旧日志「复活」

## 1.6.7 - 2026-09-08

- 筛选成本/用量可见性：结果页新增用量行（本轮 LLM 调用次数、输入/输出 tokens、估算费用、缓存命中人数），评分进度文案同步追加
- 命中评分缓存的人单独计为「缓存命中（未调模型）」，不再混入调用次数
- 真实模型调用逐次累计（含解析失败自动重试），断点续筛/刷新后续筛不断账
- 新 lib/usage.js：内置常见模型价目表（MiniMax-M2.7 2.1/8.4 元每百万等，USD 定价按 7.2 折算），未收录模型显示「未收录单价，未估费」而不是瞎估
- 设置页新增可选「自定义单价（元/百万 tokens）」：留空走内置表，厂商调价或中转价不同时填这里最准
- 兼容自定义/本地 LLM 端点不返回 usage 字段的情况（仍计入调用次数，token 记 0）

## 1.6.6 - 2026-09-08

- 修复「经历证据」折叠按钮点击冒泡：展开/收起不再误触结果行跳转到候选人详情页

## 1.6.5 - 2026-09-08

- 「具备」列相关性修复：只显示与岗位直接相关的亮点（highlights），不再混入与岗位无关的经历罗列
- 经历证据（experienceEvidence）从「具备」列拆出，改为结果卡内独立的「经历证据(N)」折叠区，默认收起、可展开核对
- 「具备」列无相关亮点时给出空态提示「AI 未找到与岗位直接相关的亮点」，与「未体现」对比语义完整
- 评分提示词校准：experienceEvidence 只写岗位职责/重点看直接相关的证据；highlights 必须「能力点＋简历对应证据」并挂靠 JD，禁止写与岗位无关的泛泛优点
- 语言类条件（英语流利/英语加分等）接受可核对的行为证据：英文翻译、英文文档/邮件产出、英语环境实习即视为具备，不再因缺证书编号误判未过
- 评分缓存版本 v4→v5，新提示词对既有候选立即生效（旧缓存自动失效）

## 1.6.4 - 2026-09-08

- 批量推进：捕获 Moka 分配接口模板，结果页勾选一键推进给本岗分配对象
- 分配对象：弹窗人选姓名→id 直采（React fiber 成对优先 + 成员映射兜底），「确认本岗分配对象」按职位永久记住，批量推进前一致性校验
- 分配对象按职位锚定：以职位名为锚（页面 URL title 与下拉框文案同源），换职位显示/确认/批量推进各自独立，跨职位时拒绝采纳页面弹窗人选
- 「重新读取」永远比对弹窗当前选择与已存记录（只读刮取），不一致即时提示并可一键采纳
- Moka 页面切换职位后，「分配对象」跟随页面职位自动刷新，不再停留在上一个职位
- 旧分配存档自愈：缺职位名章的记录在访问对应职位页面时自动补章，修正历史串岗归属
- 排查快照：设置页新增「复制排查快照（分配对象）」，一键导出页面上下文/职位名映射/存档摘要/请求流水
- 配置页联动：「按 JD 刷新」一次完成岗位理解 + 硬性门槛（学历/院校/经验/性别/实习/语言）+ 筛选关键词预设（去掉独立预填入口）
- 成员姓名收割增强：兼容 XHR json 响应、上限提至 600KB、姓名字段放宽、member-harvest 诊断日志

## 1.6.3 - 2026-09-03

- 门槛 + 经历匹配计分，加分看最多 5 项
- 岗位理解与所选职位联动，拒绝跨岗复用 JD
- 模型 JSON 失败分类、筛选进度可读、最小 CI
