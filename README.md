# Moka 智能简历筛选助手

Chrome 插件：在 Moka 候选人列表页批量拉取简历，核对硬性门槛，并用 AI 评估岗位匹配度与推进档位。

## ✨ 主要功能

- **接口拉取候选人** — 捕获并重放 Moka 列表接口，支持分页与「本轮筛选人数」
- **详情补全经历** — 自动补全实习 / 项目等结构化经历（`practiceInfo` / `projectInfo` 等）
- **硬性门槛** — 学历、院校、经验、性别、年龄等由本地判定；语言、专业及其他由 AI 逐条核对
- **筛选关键词** — 「重点看」定义经历匹配；「加分看」最多 5 项，有证据时每项 +3
- **AI 经历匹配** — 只评价经历、技能与 JD 的对口程度；门槛通过不托到 50 分
- **结果侧栏** — 左侧大数字是用于排序和推进判断的决策分；仅在门槛压分时补充经历匹配，不挡住 Moka 名单
- **多 Provider** — OpenAI / Claude / 自定义（OpenAI 兼容）接口
- **飞书协同** — 整轮筛选完成自动私聊推送汇总卡片；点卡片「一键批量推进」直接在已打开的 Moka 页面执行，完成后自动刷新并把「推荐给谁 + 被推送人名单」回执到飞书
- **健康检查** — 标签栏「健康检查」一键体检：评分模型配置 / 部署态 / 本地 Bridge / 飞书凭据 / Moka 页面连接 / 本地存储，红黄绿分级 + 每项「怎么办」指引，可一键复制结论
- **长请求可见、超长输入有上限** — 评分/补全中的卡片显示「已等待 X」；失败原因写明白（如「模型 150s 未响应，已重试 1 次」）；附件简历长文本进提示词前按上限截断（并写日志），避免网关长时间不返回导致超时
- **简历推荐对象确认章** — 「确认本岗简历推荐对象」拍板的名单快照随存档保存：批量推进一律以确认名单为准，页面手动操作不得静默替换；飞书回执标注名单来源（确认于 … / 未补章提示）
- **本地存储配置** — API Key 只保存在浏览器本地；本岗配置点保存或开筛时落盘；设置页内直接给出**内部网关取 Key** 与**飞书开放平台**的入口，不用另找地址

## 🆕 当前版本要点（v3.6.3）

- **v3.6.3（当前）**：**把「怎么装本地 Bridge」这条路补上**——此前本地 Bridge 一直没有正规分发渠道：`npm run pack` 的发布包里只有 27 个插件运行时文件，**不含 `feishu-bridge/`、也不含那 4 个 `.command`**，同事只能靠手工搬整个仓库文件夹；而且插件内所有指引都教「**双击** `启动飞书机器人.command`」，而**下载来的未签名脚本被 macOS 判为「无法验证的开发者」而拦下**（用户实际反馈）。本次三件事：
  - **新增完整包** `npm run pack:full` → `dist/moka-resume-plugin-vX.Y.Z-full.zip`：插件 + `feishu-bridge/`（server.js / package.json / package-lock.json / config.example.json）+ 4 个 `.command` + README / 使用说明。**白名单列举式打包**，`config.json`（含明文 App Secret）、`bridge.log`、`server.pid`、`node_modules` 有黑名单门禁拦着，绝不进包；实测 `.command` 的执行位在 zip 往返后保留
  - **安装主路径改成终端一条命令** `bash 安装开机自启.command`——终端执行不去问 Finder/LaunchServices，**同一份文件不被 Gatekeeper 拦**；`安装开机自启.command` 本身就自带「查 Node → 补依赖 → 注册 LaunchAgent → 启动」全流程，所以零开发成本。插件内的指引（状态行 / 健康检查「怎么办」 / 飞书卡片脚注 / 设置页说明区）**一律不再教双击**
  - **使用说明新增 §9c「飞书 Bridge 安装（可选）」**：先讲清「不装也能收卡片，只是点按钮不推进」；再给完整包 + 一条命令 + **每人自建应用的 6 步图文**（创建应用 → 拿 App ID/Secret → 开机器人能力 → 加权限 `im:message` 或 `im:message:send_as_bot` → 发布版本并把自己加进「可用范围」→ 填进插件）；附**报错对照表**（230006 未开机器人能力 / 230013 不在可用范围 / 230027 缺权限）与「双击被拦的两种放行办法」
  - 顺带确认：Bridge 会**自动把凭据持久化到 `config.json`（0600）**，同事在设置页填一次即可，不必手改文件
- **v3.6.2**：**设置页能直接跳到凭据申请页**——凭据字段原本只告诉你要填什么、不告诉你去哪拿。现在 **API Key 下方**给出「还没有 Key？去内部网关获取」（公司内网 model router 控制台 `model-router-dashboard.meitu.com/api-keys`），**飞书 App ID / App Secret 下方**给出「还没有 App ID / Secret？去飞书开放平台获取」（`open.feishu.cn/app`）；两条都是**新标签打开**的外链（带 `noopener noreferrer`），样式沿用插件扁平语汇（蓝字 + hover 下划线 + 右上小箭头），**部署态下依然可见**（藏在 `#conn-manual-fields` 之外）。顺带清掉 5 条已无任何消费者的旧凭据样式
- **v3.6.1**：**健康检查版面重做**——原来每项一张卡、整块铺绿/黄/红底加深色实心徽标，属看板式语汇，与插件「扁平白底 + 细线 + 状态点 + 描边小标签」的调性不搭。现改为：顶部**汇总条**＝设置页「本地服务状态条」同款（中性底 + 7px 状态点，点色随最严重级别）；下面**一张描边容器 + 细线分隔的六行清单**；级差只靠**状态点 + 描边小标签**（沿用飞书卡片标签形制），**行底不再铺色**，异常项把名称转红；**「怎么办」单占一行**（琥珀色），不再和现状描述挤在一段；版本/时间/「只读体检」收成一行页脚，「复制结果」的回显移到按钮旁边
- **v3.6.0**：**健康检查挪进插件本体**——原来的「🩺 健康检查」是标签栏右侧一个链接，要新开一个网页才能看，与插件割裂。现在它是**标签栏第 5 个标签「健康检查」**，切过去即自动体检，和配置 / 结果 / 设置 / 关于同一套导航；独立页 `popup/health.html`、`popup/health.js` 一并删除，不留第二份口径。同时把体检做得更好用：顶部**结论汇总条**（`✅ N 项通过 · ⚠️ N 项注意 · ❌ N 项异常` + 一句结论）、**「复制结果」**把六项结论按原话拼成纯文本、六张卡顺序固定不再抖动、连点「重新检查」时旧一轮迟到结果不回填；并**修掉一处误报**——部署态（有 `config.local.js`）下 Endpoint / 模型名存在本地私有配置、不在浏览器存储里，旧页会误报「为空」的黄项，现读设置时合并部署锁定项
- **v3.5.0**：修复「确认的简历推荐对象是 4 人，飞书一键批量推进却推给了罗耀钏」——根因是该 Moka 租户**多个职位共用同一 pipelineId**，页面手动推荐操作会带着弹窗预选的别岗人选、以本岗名章**占住主键记录**，而批量推进的查档口径（信主键）与配置面板（全表扫描同名）不一致。现落 **5 层防御**：确认章（确认名单快照入存档）→ 查档统一口径（全表扫描 + 确认章优先 + 移除内存快速路径）→ 捕获守卫（页面操作不得静默覆盖确认名单）→ 执行前最终守卫（名单与确认章不符拒绝推进）→ 飞书指令带职位身份（页面职位对不上卡片职位直接拒绝）；回执标注名单来源（确认于 … / 未补章）。**升级后请到每个岗位点一次「确认本岗简历推荐对象」补章**（旧记录无确认章，保护暂不生效）

- **v3.4.1**：修复「个别候选人一直停在 ② AI 评分中、约 5 分钟后失败」——根因是**进提示词的输入体量没有上限**（附件解析出的简历长文本整段进 prompt，网关长时间不返回 → 150s 超时）。现给输入加三级上限（简历原件 8000 字 / 单条经历 1200 字 / 每类 12 条 + 画像总长兜底），截断如实写日志（含原始字数）；**超时类失败不再进「自动补评」**（主轮内已有两次完整尝试，重复长跑只是再空等 5 分钟），单人最坏等待从 ~10 分钟降到 ~5 分钟，跳过时明确提示「点卡片「重评」重试」；失败原因改为自解释文案（`请求超时（模型 150s 未响应，已重试 1 次）`）；**结果卡显示「已等待 X」**，长请求期间不再像卡死
- **v3.4.0**：结构性批次——popup.js 拆为「主体配置 / popup-results.js 结果页 / popup-batch.js 批量推进与推荐对象」三个模块（行为不变）；弹窗芯片刮取移入 `lib/moka-dom-adapter.js`（Node 可行为测试）；新增 **🩺 健康检查页**（配置 / Bridge / 飞书凭据 / Moka 页面 / 存储六项体检，红黄绿卡片 + 怎么办指引）；新增 **E2E 冒烟链**行为测试（弹窗 → 刮取 → 采信 → 模板 → 重放 → 回执八环）
- **v3.3.1**：Bridge（feishu-bridge）可测性 + 安全加固——server.js 可被 require 而不自动启动（`require.main` 门卫 + 导出内部）；WebSocket 鉴权可升级为**扩展 ID 精确校验**（配置 `allowedExtensionId` + 插件连接先握手 `bridgeHello` 上报 ID，双重核对不符即断）；config.json（含 App Secret）落盘权限收紧 0600；飞书卡片回执两条路都失败时 3 秒重试一次；bridge.log 超 2MB 启动脚本自动归档轮转
- **v3.3.0**：正确性加固第 1 批——推荐对象/岗位预设的存储写入**全链路串行化**（content 与 popup 各自引入写队列，消灭「确认 vs 自动存档」并发互相覆盖的窗口）；写入失败（`runtime.lastError`）**如实上报**，状态行不再假绿；批量推进模板加**名章校验 + 按职位名改道**，SPA 切岗瞬间（pipelineId 已变、职位名未变）不再拿错上一岗的模板；飞书推送选 tab 优先按 URL query 里的 `pipelineId` 精确匹配，多岗同开不再打错页面
- **v3.1.1 – v3.2.2**：推荐对象与批量推进的一轮实战收敛——确认结果**就地写回状态行**（成功原地变绿，不再发底部浮动提示）；刮取出口丢弃芯片容器拼接串（不再把 4 个人显示成一坨）；切岗串岗三处加固（查档 id 兜底加名章守卫、写入冲突**别名键保留**以当前页面为准、弹窗人选按集合比对）；新岗位**免真发**——确认时自动合成默认推进模板直接建记录，真发一次仅作简历类型/抄送偏好校准；「关于」页核心能力清单纳入版本同步守护
- **v3.1.0**：「简历推荐对象」的确认反馈**就地显示**——点「确认本岗简历推荐对象」后结果原地写回状态行（成功变绿 + 闪一下，失败橙色 + `✗ 原因`），不再另发底部浮动提示（同一句话两处出现，位置还不在用户看的地方）。同时修掉姓名被显示成一坨的问题：标签邻域会把「推荐到」芯片的**容器**整串收进来（如「陈晓庆万树吴彦霖李琼」），导致人数虚高、与已记录永远比对不一致；现在在刮取出口即丢弃拼接串，人数门 / 比对 / 展示三处同时变干净
- **v3.0.2 – v3.0.9**：飞书协同推送与「简历推荐对象」的一轮集中加固——**Bridge 开机自启**（LaunchAgent 常驻，退出/崩溃自动拉起）+ `bridge.log` 时间戳与全链路埋点；修「点飞书卡片没反应」（MV3 Service Worker 半开死链：插件侧 alarms 自愈 + Bridge 侧死链探测）、「一次点击收到 2 条结果通知」（回执只发一份）、「飞书推进后插件仍显示待处理」、**切岗后推荐对象仍显示上一岗面试官**（查档两遍制：精确同名优先，近似命中显式告警）、「弹窗明明开着却提示未检测到弹窗」（只读比对不再因数量门误拒）
- **v3.0.1**：修复「一键批量推进」点击无响应、无回执——根因是 `feishu-bridge` 未安装 `@larksuiteoapi/node-sdk`，飞书长连接从未建立，卡片回调收不到（汇总卡片能收到是因为它走 OpenAPI 直发，不依赖 SDK，极具迷惑性）。**新环境部署务必 `cd feishu-bridge && npm install`**。同时删净「备用：新页面执行」整条链路（按钮 / 文案 / URL 动作通道），卡片只留「🚀 一键批量推进 + 📋 查看候选人列表」；`check:version` 门禁加固：CHANGELOG 顶部条目必须等于 manifest 版本
- **v3.0.0**：整体取消「飞书结果推送目标」——桥接无法自定义发送给对应面试官，多目标库只会制造「配了但发不到」的错觉。推送链路收敛为唯一一条：设置页配好自建应用（App ID / Secret）+ 接收人 → 机器人私聊推送给你绑定的账号；目标库 / 备用 Webhook / 多类型分流全部删除
- **v2.2.0**：飞书「一键批量推进」不再新开网页——改为卡片回调按钮，直接在已打开的 Moka 页面执行；完成后自动刷新页面，回执写明推荐对象与被推送候选人名单（超 30 人截断）；移除只存不读的「单人实时推送」设置
- **v2.1.0 / v2.0.x**：推送目标语义修正（配置不全明确报错、绝不静默改道）；修复卡片按钮 URL hash 被 SPA 路由冲掉的竞态、MV3 Service Worker 空闲挂起导致的 Bridge 反复断连（20s 应用层心跳）、汇总卡片截断口径说谎；Bridge WebSocket 增加 Origin 白名单；移除候选人 PII 兜底发给「最近私聊者」的隐私风险

### v1.10.x 及更早（点击展开要点）

<details>
<summary><b>v1.10.1 及更早版本要点</b>（点击展开）</summary>

- **「简历推荐对象」指引补齐（1.10.1）**：未记录时的提示直接点出 Moka 的实际入口——先在本职位的简历列表页勾选候选人、点「推荐给用人部门」打开弹窗（选好人即可，不用真发出去），再回设置页点「重新读取」；批量推进面板与确认流程的文案同步改口径。点「重新读取」时状态行会闪一下作为反馈（此前读取很快且文案没变化，看起来像「点了没反应」）。使用说明新增「7b. 配置简历推荐对象（批量推进前置）」完整步骤
- **设置页只需填 API Key（1.10.0）**：有 `config.local.js` 时**模型名称也纳入本地锁定**（此前只有协议 / 提供商 / Endpoint 锁定、模型名仍要手动确认），后台强制覆盖，部署信息不可能被误改。四个锁定字段不再平铺，收成一条只读**部署摘要**（模型 · 提供商 · 协议 · Endpoint + 「你只需填写下方 API Key」）；评分并发 / 强制 JSON / 费用单价三项有可用默认值，统一收进「高级」折叠区默认收起（**自己改过其中任一项会自动展开**）。没有 `config.local.js` 时行为完全不变，仍是完整表单
- **修复「推荐给用人部门」点错按钮（1.9.9）**：插件在 Moka 上点「推荐」时，实际点中的是页面上的导航入口「进入用人部门筛选」——因为确认文案表里混进了这条导航文案，而旧打分让「精确匹配」压过了「优先级靠前的具体文案」。现改为**模式顺序即优先级**（具体文案优先），确认按钮只在**弹窗/浮层**里查找，容器命中时也只点「自身文案命中」的子按钮；同时每次 Moka 操作都会把命中元素写进运行日志，方便定位误点
- **修复折叠区自动缩回（1.9.8）**：筛选进行中，结果卡的「经历证据」「评分明细」点开后会被自动收起——根因是列表整表重建时展开状态只存在 DOM 里，而筛选期间进度快照 / 重评都会触发重建。现改为状态外置保存、建卡回填，并在切换职位时清空作用域
- **历史偏好文本口径修正（1.9.7）**：归一化新增**书写形态折叠**（全角 → 半角；中文与「含数字的字母数字串」之间去空格，中文与纯英文词之间保留空格）。`日语 N1` / `日语N1` / `日语　Ｎ1` 现在算同一个信号——此前这类打字差异会让同一条件各自 count=1，被「出现 ≥2 次」的门槛滤掉（实测「未过门槛 日语N1 ×2」整条丢失）。历史偏好块里的门槛条目统一为归一后文案并去重，差距文案改用「只折叠形态、不剥语义前缀」的展示归一——`缺少甲方品牌经验` 不再被剥成读起来像优点的 `甲方品牌经验`。附带修 `normalizeMustHaveLabel` 前缀短路（`缺乏XX` 不再被剥成 `乏XX`）。代价：**有决策记录**的岗位评分缓存一次性失效，无决策岗位不受影响

- **清理死代码 + 上死代码门禁（1.9.6）**：删除 5 个「定义了但从没被调用」的符号（含 23 行已失效的旧版评分说明段），收敛 40 个无人消费的 lib 导出（221 → 181）；新增门禁测试断言「lib 导出必须在别处被引用」，`eslint no-unused-vars` 管不到的这类死代码从此会让 `npm run check` 直接失败。无行为变化、评分缓存不受影响
- **匹配分不再虚高（1.9.5）**：新增「给分锚点」+「理由与分数同向」——四项按证据格局分档（仅相邻/可迁移职责 → 40–50、行业差异大 → 45 以下、关键技能无证据 → 40 以下），理由写「缺少直接经验 / 差异较大」时该项不得超过 50。修复「理由全在说缺口、分数却给 55」的中性化打分。代价：评分缓存一次性失效（`PROMPT_VERSION` v3）
- **校准面板分组 + 近期优先（1.9.4）**：本岗校准建议拆成「规则调整」（可采纳）与「诊断」（只提示，如插件判定偏严 / 偏宽）两组，不再混排；复盘样本按更新时间倒序参与并列打破（不做时间衰减，不改阈值）
- **历史偏好不会变成硬性门槛（1.9.3）**：注入评分的历史偏好说明段明确「历史反馈只用于校准本岗偏好，不得据此创建新的硬性一票否决条件」「简历未提及某项不等于不具备该项」，避免「以前淘汰过类似的人」被误解成一票否决；全岗「反复信号」也改用归一化口径统计。代价是评分缓存一次性失效（`PROMPT_VERSION` v1→v2），下次筛选会重新调用模型
- **本岗校准更准、不误导（1.9.2）**：校准建议先做**信号归一化**再聚合——「没有达人资源 / 缺少达人合作经验 / 无KOL合作经验」算同一个「达人合作」，反复偏好不再被不同说法拆散；写入「重点看」的是**肯定式**文案（不再写「无达人合作」这种反义句）；新增**偏向诊断**（插件判定可能偏严 / 偏宽，只提示不可采纳），替代过去「明明有系统性分歧却报判断较一致」；每条建议附**可信度 高/中/低**；`加入门槛` 会提醒「这是不是真正的一票否决条件」
- **内网连接锁死、模型名放开（1.9.1；1.10.0 起模型名也纳入锁定）**：有 `config.local.js` 时，接口协议 / API 提供商 / Endpoint 三项被锁定为内网值（设置页置灰，防误改）；**模型名称改为默认值兜底，设置页可随时切换**（如同网关的不同模型档位），改完即生效
- **连接配置全面自由化（1.8.9）**：「接口协议」（OpenAI 兼容 / Anthropic Claude）是唯一硬约束；**API 提供商改为自由文本框**（选常用服务自动带出 Endpoint 与模型名，仍可任意改）；**API Endpoint 常显可编辑**（官方 / 中转 / 自建 / 本地 localhost 都行，只填到 `/v1` 会自动补 `/chat/completions`）；API Key 与模型名一如既往自由填写。老配置自动迁移，无需重填
- **结果页首行 = 用时 + 预估花费（1.8.8）**：「用量」行改为「用时 2 分 41 秒 · 预估花费 ¥0.156」——整轮耗时秒级实时刷新、完成即冻结，调用次数与 token 收进运行日志不再上结果页；模型未收录单价时整行隐藏
- **并发降档抗抖 + 可见（1.8.7）**：并发自适应不再「一次抖动就降档」——需连续 2 次过载/超时（中间无成功）才降一档，一次正常出分即清零失败计数，避免偶发 429 把并发从 6 误降到 4；开筛/续筛时运行日志会写明「评分并发 N（设置上限 M）」，降过档时标注原因
- **并发自适应（1.8.6）**：遇到网关 429/5xx 或调用超时自动把并发降一档（8→6→4→3→2），连续成功 10 次再逐档回升；设置页的并发数作为上限。配合单次调用超时 150s、超时最多重试 1 次，避免网关被重试打爆
- **修复双跑（1.8.6）**：续筛过程中重新点「开始筛选」，旧续筛 worker 现在会被立即叫停，不再出现同一候选人被评两遍、并发翻倍
- **失败可见（1.8.6）**：评分失败会标明类型（timeout / overload / network / config / other），不再只是「错误 0 分」
- **评分稳定性（1.8.5）**：评分输出上限提到 8000 tokens（思考型模型如 MiniMax-M2 的思考内容也占这份预算，4000 容易把 JSON 挤断）；识别到 `finish_reason=length` 截断时自动把上限加倍重试一次；自定义/中转端点默认带 JSON 模式，网关不支持时自动去掉该参数降级重试；未闭合的思考块不再被当成 JSON 解析
- **并发可配置（1.8.5）**：设置页「评分并发数」可选 1–8（默认 6），自建网关或本地模型可调大提速，被限流时调小
- **评分诊断日志（1.8.5）**：每条评分日志附输出长度、截断标记、是否含思考、调用次数与解析失败类型，慢与失败一眼可定位
- **单人重评修复（1.8.5）**：此前「重评」把内部包装对象当成模型输出，必然判成评分失败；现已修好
- **结果卡右列「差距」（1.8.4）**：原「未体现」只覆盖「简历没写」一层，而该列还装着确认未过的门槛与待确认项；「差距」与左列「亮点」正反对仗，细分类仍由红色「门槛」/琥珀「待确认」徽章承担
- **配置页扁平分区（1.8.2）**：五个分区去掉卡片壳，改「SVG 图标 + 标题 + 细分割线」连续分区，内容区更高；标题图标全部换成单色线性 SVG；10px 文字地板抬到 11px，数字等宽对齐
- **结果卡细节（1.8.3）**：左列由「具备」改名「亮点」；候选人首次出分时卡片淡入、分数徽章轻弹入（只在「评分中→已出分」时播一次，系统开启「减弱动态效果」时禁用），进度条运行中有低对比流光表示活着；操作按钮 emoji 全部换单色线性 SVG
- **评分体系：四项结构化 + 本地加权合成（1.8.0）**：模型分别给「核心职责 40% / 业务场景 25% / 专业技能 20% / 责任范围 15%」打分并各带理由，插件按固定权重本地合成决策分——分数可解释、不随模型漂移；核心职责弱时保护封顶（<40→≤49、40–59→≤69、60–79→≤84），title 像但活不对口不会被误推
- **门槛三态（1.8.0）**：门槛判 pass / fail / unknown；简历没提及=unknown，不脑补、不扣分，卡片以琥珀色「待确认 · ×××」标出待人工核对项，与红色「未过门槛」区分
- **卡片把握度指标（1.8.0）**：卡面常显「简历信息完整度 N%」与「判断把握 高/中/低」；四项分数与理由收进「评分明细」折叠区，卡面只留结论
- **职位配置存档与自动回填**：每个职位的岗位理解 / 硬性门槛 / 简历关键词自动存档；刷新页面、关闭插件后再进同一职位自动带出。存档带职位身份锚点 + 恢复诊断 + 串档防线（锚名不符拒填、空档按首次进入重新生成），保证「存的哪个岗、回填就是哪个岗」
- **简历推荐对象按职位闭环**：推荐对象按职位锚定，Moka 切页后跟随刷新，旧记录缺职位名章时当场自愈；批量推进按捕获职位重放，不跨职位串岗
- **运行日志与排查快照**：侧栏「运行日志」会话级留存（请求 / 分配 / 筛选 / 评分 / 错误分类，可筛选 / 复制 / 导出）；「复制排查快照」一键导出页面上下文 + 职位存档锚点 + 完整接口流水
- **LLM 用量与费用可见**：结果页显示本轮调用次数 / token / 估算费用（内置价目表，设置页可填自定义单价覆盖）；评分缓存命中单独计数，断点续筛不断账
- **配置页体验**：卡片化分区、硬性门槛两列网格、年龄要求下拉式多选（20-25 / 25-30 / 30-35 / 35+）、「💾 保存并测试」一步完成
- **稳健性加固（1.6.18–1.7.1）**：切岗互斥队列、侧栏初始化分步容错、停止后屏蔽在途评分回写、分页抓取失败自动重试退避、热更新幂等守卫（不二次劫持页面请求）、importScripts 逐个容错、并发评分去重（同候选人并发只调一次模型、不重复扣费）、顶层 DOM 绑定空值守航（safeEl）、XHR 监听防泄漏、lint 门禁覆盖全部入口文件 + no-undef 锁死未定义标识符、桥接消息 nonce 握手防伪造、存储写入失败可观测（跑 `npm run check` 自动执行）
- **CSV 导出增强（1.7.2）**：已决策简历导出新增「同步状态」「决策时间」两列，文件名带职位名，多职位分别导出不再混淆
- **进度口径统一（1.7.3）**：筛选中顶部摘要与底部进度条按同一口径计数（已处理含评分失败），并发评分时进度文案改为「已评 N/M · 刚完成 ×××」不再误导
- **评分失败自动补评（1.7.4）**：筛选结束仍有失败项时自动再补评一轮；解析失败触发的重试附带「严格只输出 JSON」纠偏指令；评分调用固定 `temperature: 0`
- **简历详情标签复用（1.7.5）**：点卡片打开候选人简历复用同一个标签页，不再每点一人新开一页
- 档位统一「不建议推进 / 可推进 / 优先推进」

</details>

<details>
<summary><b>历史版本要点（v1.6.3 及更早）</b>（点击展开）</summary>

### v1.6.3

- 硬性门槛 + AI 经历匹配：门槛未过时决策分封顶 49；门槛全过或未配置时，决策分等于经历匹配
- 推进档位统一为「不建议推进 / 可推进 / 优先推进」
- 预填写入表单不自动落盘；点「保存当前筛选条件」或开筛时落盘
- 本岗校准、已决策历史、待处理筛选等既有能力保留

### v1.5.12

- 筛选反馈：结果卡片可标「要沟通 / 不考虑」，本地保存并注入下次 AI 评分以校准推荐
- 重评修复：评分失败卡片可单人重评，侧栏有进度与错误提示
- 关于页版本号自动读取 `manifest.json`，与 Chrome 扩展页显示一致

### v1.5.10

- 名单只按综合分排序；硬性未过每条 −5（其他必备最多 6 条、满扣 −30）。忽略自增项会加回扣分；学历/院校等不能忽略

### v1.5.9

- 同岗位的硬性要求和评分权重自动保存在本地，下次打开直接恢复，不用再点预填或生成权重

### v1.5.8

- 「推荐」按综合分 ≥ 50；硬性未过仍可进推荐，卡片上红标标出未过项

### v1.5.7

- 同方向但不完整（年限不够、渠道只覆盖一部分）按「值得翻一翻」给分，不再压到 20 分档；专业不对口也不再把教育打穿

### v1.5.6

- 经验分区分对口与擦边：实习岗里行政实习顺带做招聘，不再打成对口 HR 经验（擦边最高 55）

### v1.5.5

- 「按 JD 生成建议权重」放在评分权重标题右侧，只改四个滑块，不再预填其他必备或关键词

### v1.5.4

- 「按 JD 预填」覆盖硬性要求全部字段：学历、院校、性别、年龄、经验/实习、其他必备、简历关键词

### v1.5.3

- 硬性要求标题右侧可点「按 JD 预填」，从当前职位 JD 识别学历与经验（本地解析，不打 LLM）

### v1.5.2

- 结果卡片把亮点 / 差距拆成左右两列：「具备」（绿）与「未体现」（红）
- 自增硬性未满足会出现在右侧细项里（带「忽略」），学历等结构化缺项仍在上方红标；分数仍不另扣

### v1.5.1

- 硬性要求可自增「其他必备」；未满足与学历未过一样置底，不再额外扣 5 分
- 「按 JD 生成建议」预填时会去掉已与学历/年限/性别等选项重复的条目

### v1.5.0

- 点扩展图标打开 Chrome 侧栏：配置、进度、结果在同一处，点页面不会关掉
- 页面上不再盖结果浮层，Moka 名单保持完整可见；点侧栏卡片仍会打开对应候选人

### v1.4.5

- 简历关键词接到筛选：JD 预填、可增删；未命中在结果里标黄，不一票否决
- 必备项可编辑，点「按 JD 生成建议」会预填，再交给 AI 判定
- 结果浮层可按 推荐 / 硬筛未过 / 评分失败 过滤，并搜索姓名
- 评分失败的卡片可单人「重评」，不必整轮重来

### v1.4.4

- 打开弹窗不再自动解读 JD；需要时点「按 JD 生成建议权重」
- 评分 / JD 缓存写入本地存储，扩展重启后同一人同一岗位不重复扣费
- 结果浮层可导出 CSV；同一职位可「查看上次结果」

### v1.4.3

- 自动探测详情接口会带上列表请求的鉴权头，并尝试从页面 / 其它接口收割 `scene`
- 探测失败时横幅改为「自动识别未成功」，状态栏带上失败原因（如 HTTP 403），不再像没试过一样只说「建议先点开」

### v1.4.2

- 解析失败 / 未配 Key 记为「错误」，不再当成约 50 分的「值得推荐」
- 评分时把招聘官勾选的硬性条件一并交给模型，减少和本地硬筛打架
- 开始筛选时自动探测详情接口；捕获结果在浏览器会话内保留；补全经历失败会换路径重试

### v1.4.1

- 单人评分失败不再卡死整轮：记错误分并继续下一位
- LLM / Moka 请求增加超时，避免一条挂住堵住筛选
- 评分与 JD 缓存增加容量上限，降低长时间大批量内存膨胀
- 新增 `config.local.example.js` 本地配置模板

### v1.4

- 必备项扣分摊开展示：「四维加权 − 必备项扣 = 综合分」
- 支持单人单条「不扣 / 恢复」，仅影响该候选人并重排
- 移除未实现的「自动保存评分到备注」高级开关

</details>

更细的操作说明见仓库内 [`使用说明.html`](./使用说明.html)（可用浏览器直接打开）。

## 📋 系统要求

- Chrome 116+
- 可用的 LLM API Key（OpenAI、Claude 或 OpenAI 兼容服务）
- Moka 账号，并在 `https://app.mokahr.com` 的**候选人列表**页使用
- （可选）飞书协同：自建飞书应用（App ID / App Secret，**建议每人自建一个**）+ 本地 Bridge（Node 18+，只监听 `127.0.0.1:18888`，多人各装一份互不干扰）。Bridge 装在**完整包**（`*-full.zip`）里，安装方式见 [`使用说明.html` §9c](./使用说明.html)：

```bash
# 在项目文件夹的终端里执行（推荐用终端，不要双击——macOS 会拦「下载后双击的未签名脚本」）
bash 安装开机自启.command   # 自动补依赖 + 注册开机自启（LaunchAgent）+ 启动
bash 停止飞书机器人.command # 临时停止
bash 取消开机自启.command   # 移除开机自启
```

- **不装 Bridge 也能用**：整轮汇总卡片照常私聊推送；只有「在飞书点卡片按钮直驱 Moka 页面批量推进」这一步需要它

开发时：Node 22+，`npm ci && npm run check`。发版：`npm run pack`（精简包，只有 Chrome 要加载的运行时文件）、`npm run pack:full`（完整包，额外含 `feishu-bridge/` 与 4 个 `.command`，发给同事用这个）。产物在 `dist/`。

## 🚀 安装步骤

### 1. 克隆或下载

```bash
git clone https://github.com/lzm42727-netizen/moka-resume-plugin.git
cd moka-resume-plugin
```

### 2. 在 Chrome 中加载

1. 打开 `chrome://extensions/`
2. 开启「开发者模式」
3. 「加载已解压的扩展程序」→ 选择本项目文件夹
4. 代码更新后：扩展页点「重新加载」，并**刷新 Moka 页面**

### 3. 配置 API

1. 打开 Moka，点击工具栏插件图标（打开右侧栏）→ 「设置」
2. 填写提供商、Endpoint、API Key、模型名称
3. 填好后点「💾 保存并测试」：保存设置并自动测试 API 连接，成功/失败都会在按钮旁提示

自定义 Endpoint 可填到 `/v1`；插件会按需补全 chat 路径。首次访问新域名时，请允许浏览器弹出的 host 权限。

## 📖 使用指南

1. 打开目标职位的**候选人列表**（如初筛）
2. **（可选）先点开任意一位候选人姓名进入详情一次**，再回列表——自动探测失败时，这样能捕获详情接口（含 `scene`），保证实习 / 项目经历能补全
3. 点扩展图标打开右侧栏 → 「配置」：选职位、正式员工 / 实习生、本轮人数
4. 「岗位理解」可按 JD 刷新；硬性门槛区可预填学历、语言、专业等条件；维护「重点看 / 加分看」后点保存，或直接开筛（会自动落盘）
5. 「开始筛选」，在侧栏「结果」里查看评分；可「导出 CSV」；可用「本岗校准」。点卡片会在 Moka 打开该候选人

## 🎯 评分说明

| 决策分 | 档位 | 说明 |
|---------|------|------|
| 80–100 | 🟢 **优先推进** | 门槛已过且匹配明显强 |
| 50–79 | 🔵 **可推进** | 门槛已过且匹配尚可 |
| 0–49 | 🔴 **不建议推进** | 未过门槛，或匹配分不足 |

计分规则（v1.8.x）：模型对「核心职责 40% / 业务场景 25% / 专业技能 20% / 责任范围 15%」四项分别打分，插件按该权重在本地合成**匹配分**；核心职责弱时保护封顶（`coreDuty < 40` → 匹配分 ≤ 49，`40–59` → ≤ 69，`60–79` → ≤ 84，`≥ 80` 不封顶）。门槛判 pass / fail / unknown 三态：有明确未过门槛时，决策分 = `max(0, 49 − 7 × 未过条数)`；信息不足（unknown）不扣分。门槛全过但匹配分 < 50 时，决策分 = 匹配分；匹配分 ≥ 50 且无未过门槛时，决策分 = `min(100, 匹配分 + 3 × 具备的加分看条数)`（加分最多 15，允许跨过 80 进入优先推进，卡片标注「加分晋级」）。面板「推荐 N」统计可推进与优先推进人数。

**结果卡只显示一个分**（v1.8.1）：卡面只保留决策分；匹配分、四项分数与理由、以及完整计算链（`匹配分 57 ＝ 52×40% + …`、`决策分 28 ＝ 49 − 7 × 3 条未过门槛`）都收进「评分明细」折叠区。卡面另常显「简历信息完整度」与「判断把握」两个把握度指标；未过门槛只在「差距」列出现一次（带红色「门槛」徽章，v1.8.4 起右列由「未体现」改名「差距」），信息不足项以琥珀色「待确认」标出。

## 🐛 故障排除

- **未找到候选人 / 拉取很少**：确认在候选人列表页并刷新；调大「本轮筛选人数」或选「全部」；先让列表加载出搜索请求再筛选。
- **提示先点开候选人 / 经历补不全**：插件会先自动探测详情接口；仍失败时在列表点开任意一人详情一次后重试；看状态是否出现「已补全经历 N 位」。
- **API 连接失败**：检查 Key、Endpoint、网络与配额；用「测试 API」；新域名需授权。
- **无法解析模型返回**：重新加载扩展后重筛（解析失败不写缓存）；可换指令跟随更稳的模型。
- **筛选很慢**：人数 × AI 调用量较大时属正常；可减小本轮人数或点「停止」。

## 🔒 隐私和安全

- ✅ API Key 与设置只保存在浏览器本地存储
- ⚠️ 评分时候选人画像会发送到**你配置的** LLM 服务；合规敏感场景请用内网或本地模型
- ✅ 插件不另行收集使用分析；代码可自行审计

## 🤝 贡献

欢迎提交 Issue 和 Pull Request。

## 📄 许可证

MIT License
